# EXAMPLES OF ARITHMETIC IN R
2^3  # exponents, 2^3 is preferred but 2**3 works too
11 %/% 3  # integer divison
11 %% 3  # modulo (remainder)
log(100)  # functions take input inside parentheses

# VARIABLE ASSIGNMENT USES THE ARROW: <- OR ->
snake_case <- "foo_bar"
CamelCase <- "FooBar"
dot.case <- "foo.bar"
.hidden_variable <- "top secret"


# PACKAGE MANAGEMENT
installed.packages() #what is installed
install.packages() #to install new
update.packages() #new versions 
remove.packages() #uninstall
library("ggplot2")
Yes
library("ggplot2")

paste(1, 2)

paste(c("one", "two"), c(111, 222))
